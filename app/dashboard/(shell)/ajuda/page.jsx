import HelpContent from "@/components/dashboard/suporte/HelpContent";

export const metadata = {
  title: "Central de ajuda — TironiDraws",
};

export default function AjudaPage() {
  return <HelpContent />;
}
