import AppShell from "@/components/AppShell";
import SupportContent from "@/components/suporte/SupportContent";

export const metadata = {
  title: "Suporte — RifaMaster",
};

export default function SuportePage() {
  return (
    <AppShell>
      <SupportContent />
    </AppShell>
  );
}
